
export default defineEventHandler(async () => {
  // const data = await getLastTransactions(10)
  // return {
  //   isSuccessfull: true,
  //   data: data.map((d: { maskedId: any; nominal: any; status: any; time: any }) => ({
  //     id: d.maskedId,
  //     nominal: d.nominal,
  //     status: d.status,
  //     time: d.time
  //   }))
  // }
})
